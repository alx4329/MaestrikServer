const {Author} = require('./author');
const {Book} = require('./book');
const {Publisher} = require('./publisher');
const {User}  = require('./user')

module.exports= {Author, Book, Publisher,User}


